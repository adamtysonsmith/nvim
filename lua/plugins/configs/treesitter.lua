local options = {
  ensure_installed = { "lua", "svelte", "javascript", "css" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
